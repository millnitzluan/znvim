return {
  "nyoom-engineering/oxocarbon.nvim",
  lazy = false,
  enabled = true,
  config = function ()
    vim.cmd.colorscheme "oxocarbon"
  end
  -- Add in any other configuration;
  --   event = foo,
  --   config = bar
  --   end,
}
